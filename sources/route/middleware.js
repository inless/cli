import {Plugins, Configs, logger} from 'inless';

// let myPlugin = new Plugin('my-plugin');
// let myConfigs = new Configs('my-configs');

export default class Route {
	constructor(route) {
		logger.debug(`Initializated plugin ${this.displayName}`);
	}
}

